@require "./common/variable"
// @require "./mixins/mixins"

.tulp-captcha-countdown
  cursor pointer
  font-size $_font-size
  display flex
  align-items center
  // buttonLoading()
  &.is-disable, &.is-loading 
    color $_color-text-looming
  [class*=tulp-icon] + &__text 
    margin-left 5px
