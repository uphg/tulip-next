import { withInstall } from '../../utils'
import Collapse from './src/Collapse'
import CollapseItem from './src/CollapseItem'

const TuCollapse = withInstall(Collapse)
const TuCollapseItem = withInstall(CollapseItem)

export { TuCollapse, TuCollapseItem }
