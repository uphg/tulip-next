export const maxZIndex = {
  mounted(el, bindings) {
    console.log('el')
    console.log(el)
    console.log('bindings')
    console.log(bindings)
  },
  updated(el, bindings) {
    
  },
  unmounted(el, bindings) {
    
  }
}